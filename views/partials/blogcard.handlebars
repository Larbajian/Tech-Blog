<div class="card text-white bg-secondary mb-3" style="max-width: 18rem;">
  <div class="card-header">
    {{blog.date_created}}
  </div>
  <div class="card-body">
    <h5 class="card-title">
        {{blog.title}} by {{user.name}}
    </h5>
    <p class="card-text">
        {{blog.text}}
    </p>
  </div>